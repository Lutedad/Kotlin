// 두 개의 정수 리스트를 받아서, 두 리스트의 교집합을 반환하는 함수를 작성하세요.
// 함수명: intersection
// 매개변수: 리스트 a, 리스트 b
// 반환값: 두 리스트의 교집합을 담은 리스트

// 예시
// intersection(listOf(1, 2, 3), listOf(2, 3, 4)) -> [2, 3]

fun main(){

}

fun intersection(a : List<Int>,b : List<Int>){
    a.forEach { b.forEach(it) }
}
